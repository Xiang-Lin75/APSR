{
  "model": "APSR",
  "python_class": "apsr.APSR",
  "constructor": {},
  "sample_rate": 8000,
  "checkpoint_epoch": 102,
  "training_seed": 43,
  "model_settings": {
    "n_fft": 256,
    "win_length": 256,
    "hop_length": 64,
    "stft_center": true,
    "channels": 64,
    "enrollment_tokens": 8,
    "memory_dim": 16,
    "refinement_rounds": 4
  },
  "loading": "Use APSR.from_pretrained(checkpoint_path, device). The class accepts no experimental variant configuration.",
  "historical_release": "The original E102 configuration described the research constructor. This configuration uses the renamed public APSR API; inference tensors and their digest are unchanged."
}
